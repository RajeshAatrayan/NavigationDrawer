<?xml version="1.0" encoding="utf-8"?><!--DrawerLayout to get the drawer on our screen-->
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.ibrickedlabs.navigationdrawer.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="MainLayout"
            android:textSize="24sp" />
    </LinearLayout>
    <!--you can mention left or start because you dont want the menu on the screen-->
    <android.support.design.widget.NavigationView
        android:id="@+id/navigatioView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="left"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/side_list" />


</android.support.v4.widget.DrawerLayout>
